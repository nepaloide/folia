# ── Database ──────────────────────────────────────────────────────────
# Con Docker:  no toques esto, DATABASE_URL la define docker-compose.yml
# Sin Docker:  descomentar la de abajo
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/folia

# ── Secrets (requeridos) ─────────────────────────────────────────────
SECRET_KEY=change-me-in-production
GOOGLE_CLIENT_ID=your-google-client-id
